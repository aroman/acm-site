<% 
nav_links = %w( home who\ we\ are what\ we\ do join sponsor )
nav_paths = { 
  'home' => home_path,
  'who we are' => about_path,
  'what we do' => we_path,
  'join' => join_path,
  'sponsor' => sponso_path
}
%>    
    <ul id = "nav">
<% nav_links.each do |link| %>
      <%= content_tag(:li, link_to(link, nav_paths[link]), :id => link, 
                      :class => (@title.downcase == link)? 'current' : '') %>
<% end %>
    </ul>
